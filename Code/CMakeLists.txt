add_subdirectory(angus)
add_subdirectory(bilsdale)
add_subdirectory(fremont)
add_subdirectory(emley)
add_subdirectory(palace)
IF(GODOT)
    add_compile_definitions(GODOT)
    add_subdirectory(sutton)
ELSE()
    add_subdirectory(beacon)
ENDIF()